package com.optical.sender;

/**
 * Entry point for the Sender application.
 * Uses the Launcher pattern to avoid JavaFX classpath issues in non-modular projects.
 */
public class SenderLauncher {

    public static void main(String[] args) {
        SenderApp.main(args);
    }
}
